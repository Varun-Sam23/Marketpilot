# MarketPilot

AI-powered pre-market and live market intelligence dashboard.

## Current-session orchestration
The pre-market workflow runs on weekdays at 09:00 IST and can also be triggered automatically by non-data repository updates. Data-only updates are ignored to prevent recursive workflow runs.
